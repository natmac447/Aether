---
phase: 06-user-interface
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - Source/PluginEditor.h
  - Source/PluginEditor.cpp
  - Source/PluginProcessor.cpp
  - Source/ui/SectionComponent.h
  - Source/ui/SectionComponent.cpp
  - Source/ui/PresetSelector.h
  - Source/ui/PresetSelector.cpp
  - CMakeLists.txt
autonomous: false
requirements:
  - UI-01
  - UI-06
  - UI-09
  - UI-10

must_haves:
  truths:
    - "Plugin displays 900x530px window with three-column layout (220px left, flex center, 220px right)"
    - "Left panel contains Stages I, IV, V with diamond dividers between sections"
    - "Center panel contains Stage II label, visualization placeholder, and room control knobs (Room Size, Shape, Proximity, Width)"
    - "Right panel contains Stages III, VI, Output with diamond dividers between sections"
    - "All 21 APVTS parameters are connected to their corresponding UI controls"
    - "Section labels show Roman numerals and section names in correct typography"
    - "Header shows AETHER title with letter spacing, Environment Simulator subtitle, preset selector, Cosmos Series mark"
    - "Footer shows version, tagline, and brand mark"
    - "Corner brackets, double-rule borders, and paper texture are visible"
    - "Bypassing a section visually dims that section while controls remain interactive"
    - "Plugin uses custom AetherEditor (not GenericAudioProcessorEditor)"
  artifacts:
    - path: "Source/PluginEditor.h"
      provides: "Complete editor with all section components, LookAndFeel, cached background"
      contains: "class AetherEditor"
    - path: "Source/PluginEditor.cpp"
      provides: "Three-column layout, header/footer paint, all control wiring"
      min_lines: 250
    - path: "Source/ui/SectionComponent.h"
      provides: "Reusable section container with label, bypass, controls"
      contains: "class SectionComponent"
    - path: "Source/ui/SectionComponent.cpp"
      provides: "Section layout with label row and control area"
      min_lines: 60
    - path: "Source/ui/PresetSelector.h"
      provides: "Styled ComboBox for preset dropdown"
      contains: "class PresetSelector"
    - path: "Source/PluginProcessor.cpp"
      provides: "createEditor returns AetherEditor instead of GenericAudioProcessorEditor"
      contains: "return new AetherEditor"
  key_links:
    - from: "Source/PluginEditor.cpp"
      to: "Source/ui/AetherKnob.h"
      via: "Editor creates and positions AetherKnob instances for all knob parameters"
      pattern: "AetherKnob"
    - from: "Source/PluginEditor.cpp"
      to: "Source/ui/AetherToggle.h"
      via: "Editor creates AetherToggle instances for Material, Shape, Character, Gate"
      pattern: "AetherToggle"
    - from: "Source/PluginEditor.cpp"
      to: "Source/ui/AetherBypassButton.h"
      via: "Editor creates AetherBypassButton instances for each stage"
      pattern: "AetherBypassButton"
    - from: "Source/PluginEditor.cpp"
      to: "Source/ui/ParchmentElements.h"
      via: "Editor paint calls ParchmentElements for background, corners, borders"
      pattern: "ParchmentElements::"
    - from: "Source/PluginEditor.cpp"
      to: "Source/PluginProcessor.h"
      via: "Editor accesses processorRef.apvts for parameter attachment"
      pattern: "processorRef\\.apvts"
    - from: "Source/PluginProcessor.cpp"
      to: "Source/PluginEditor.h"
      via: "createEditor returns new AetherEditor"
      pattern: "return new AetherEditor"
---

<objective>
Assemble the complete Aether UI: three-column layout with all six DSP stage sections, header/footer zones, preset selector, all 21 parameters wired to their custom controls, and all decorative elements in place. Switch the processor to use the custom editor.

Purpose: This is the integration plan -- it brings together the foundation (Plan 01), components (Plan 02), and decorative elements (Plan 03) into the final working UI. After this plan, the plugin has a fully functional Victorian parchment interface.
Output: Complete working editor with all controls connected to parameters, visible and interactive.
</objective>

<execution_context>
@/Users/nathanmcmillan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nathanmcmillan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-interface/06-RESEARCH.md
@.planning/phases/06-user-interface/06-01-SUMMARY.md
@.planning/phases/06-user-interface/06-02-SUMMARY.md
@.planning/phases/06-user-interface/06-03-SUMMARY.md
@Source/Parameters.h
@Source/PluginProcessor.h
@Source/PluginEditor.h
@Source/PluginEditor.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SectionComponent, PresetSelector, and full editor layout</name>
  <files>
    Source/ui/SectionComponent.h
    Source/ui/SectionComponent.cpp
    Source/ui/PresetSelector.h
    Source/ui/PresetSelector.cpp
    Source/PluginEditor.h
    Source/PluginEditor.cpp
    Source/PluginProcessor.cpp
    CMakeLists.txt
  </files>
  <action>
    **SectionComponent.h/cpp** -- Reusable container for a DSP stage section:

    Constructor: `SectionComponent(const juce::String& numeral, const juce::String& name)`
    - Stores numeral ("I.") and name ("CABINET RESONANCE") for label drawing
    - Owns an AetherBypassButton (optional -- Output section has no bypass)
    - Has a `setBypassed(bool)` method that triggers the dimming overlay on child controls
    - paint(): draws section label using ParchmentElements::drawSectionLabel, and if bypassed, draws 40% Parchment overlay over control area (below label row)
    - resized(): positions bypass button right-aligned at top, leaves remaining area for controls
    - Provides `getControlArea()` returning the Rectangle below the label row for parent to position controls in
    - Public `bypassButton` member for attachment by parent

    **PresetSelector.h/cpp** -- Styled dropdown for preset selection:

    Constructor: `PresetSelector()`
    - Wraps a juce::ComboBox styled for the parchment theme
    - Populates with placeholder items: "I. -- Tight Booth" through "VI. -- Church Hall" (6 presets, Phase 8 will implement actual preset loading)
    - Also add "-- Default --" as the first item (selected by default)
    - Style: EB Garamond 14px, Ink Light text, 1px Ink Ghost border, transparent background
    - Size: ~180x24px
    - For now, changing preset does nothing (preset system is Phase 8). The selector is placed in the header for layout completeness.

    **PluginEditor.h** -- Complete rewrite of the editor header:

    Members:
    - `AetherLookAndFeel lookAndFeel` -- owned, set on this component in constructor, cleared in destructor
    - `juce::Image backgroundImage` -- cached from ParchmentElements::generateBackground()
    - Stage I (left panel): SectionComponent resSection, AetherKnob resWeightKnob, AetherToggle resMaterialToggle (NOTE: Material is a 10-option choice -- use a styled juce::ComboBox dropdown instead of AetherToggle since 10 options don't fit in toggle buttons)
    - Actually for Material: Use `juce::ComboBox materialCombo` with ComboBoxAttachment, styled by LookAndFeel
    - Stage II (center panel): SectionComponent reflSection, AetherKnob reflSizeKnob(64), reflProxKnob(64), reflWidthKnob(64), ComboBox reflShapeCombo (7 named shapes -- dropdown, not toggle), and their attachments
    - Stage III (right panel): SectionComponent airSection, AetherKnob airAmountKnob, AetherToggle airCharToggle (3-way: Warm/Neutral/Cold)
    - Stage IV (left panel): SectionComponent excitSection, AetherKnob excitDriveKnob
    - Stage V (left panel): SectionComponent toneSection, AetherKnob toneAmbKnob, AetherToggle toneGateToggle (3-way: Always On/Signal-Gated/Transport-Only)
    - Stage VI (right panel): SectionComponent tailSection, AetherKnob tailDecayKnob, AetherKnob tailDiffKnob
    - Output (right panel): SectionComponent outputSection (no bypass), AetherKnob mixKnob, AetherKnob levelKnob
    - Header: PresetSelector presetSelector
    - All SliderAttachments, ComboBoxAttachments, ButtonAttachments as unique_ptr members

    **PluginEditor.cpp** -- Complete rewrite:

    **Constructor:**
    1. Set LookAndFeel: `setLookAndFeel(&lookAndFeel)` (NOT setDefaultLookAndFeel)
    2. Generate cached background: `backgroundImage = ParchmentElements::generateBackground(900, 530)`
    3. Set size: `setSize(900, 530)`
    4. Create all section components with addAndMakeVisible
    5. Create all knobs with addAndMakeVisible (inside their sections or directly on editor)
       - Standard knobs (56px): resWeight, airAmount, excitDrive, toneAmb, tailDecay, tailDiff, mix, level
       - Large knobs (64px): reflSize, reflProx, reflWidth
    6. Create ComboBoxes for Material (10 items) and Shape (7 items) with addAndMakeVisible
    7. Create toggles for Air Character (3-way), Gate Mode (3-way) with addAndMakeVisible
    8. Create bypass buttons inside section components
    9. Create preset selector with addAndMakeVisible
    10. Set up ALL parameter attachments AFTER addAndMakeVisible (per Crucible pattern, avoids silent initial sync failure):
        - SliderAttachments: resWeight, reflSize, reflProx, reflWidth, airAmount, excitDrive, toneAmb, tailDecay, tailDiff, outMix, outLevel (11 knobs)
        - ComboBoxAttachments: resMaterial, reflShape (2 dropdowns)
        - Toggle attachments: airChar, toneGate (2 toggles via hidden ComboBox inside AetherToggle)
        - ButtonAttachments: resBypass, reflBypass, airBypass, excitBypass, toneBypass, tailBypass (6 bypasses)
        - Total: 21 parameters
    11. Set up value formatters for special display params:
        - Room Size: "62% (Medium)" format -- combine percentage + descriptor
        - Proximity: "30% (Nr-Mid)" format
        - Decay: "150ms" or "1.2s" format (switch at 1000ms)
        - Output Level: "0.0 dB" format with 1 decimal
        - Width, Air, Drive, Ambience, Diffusion, Mix, Weight: "62%" whole number percent
    12. Set up bypass listeners: when a bypass param changes, call section.setBypassed() and all child knobs/toggles setBypassed()

    **Destructor:**
    - `setLookAndFeel(nullptr)` -- CRITICAL to avoid dangling pointer (Crucible pattern)

    **paint():**
    1. Draw cached backgroundImage at (0, 0)
    2. Draw corner brackets: ParchmentElements::drawCornerBrackets(g, getLocalBounds())
    3. Draw header zone (~50px):
       - Draw "AETHER" title at left (x=30, y=18) using LookAndFeel getDisplayFont(32px), 6px letter spacing via ParchmentElements::drawLetterSpacedText, Ink colour. Use the Ae ligature character in display or just "AETHER".
       - Draw "Environment Simulator" subtitle below title (x=30, y=36) using getLabelFontItalic(11px), 2px tracking, Ink Faint
       - Draw preset selector positioned centre-right of header
       - Draw "COSMOS SERIES" mark at right (x aligned ~20px from right edge) using getLabelFont(10px), 2px tracking, uppercase, Ink Faint
    4. Draw header double-rule border: ParchmentElements::drawDoubleRule(g, 50, 0, 900, true)
    5. Draw footer zone (~40px, starting at y=490):
       - Draw footer double-rule border: ParchmentElements::drawDoubleRule(g, 490, 0, 900, false)
       - Draw "Aether v0.1.0" at left (x=30)
       - Draw tagline "The invisible medium through which all sound propagates." centred
       - Draw Cairn mark or "CAIRN" text at right
       - All footer text: Spectral 9px uppercase, 2px tracking, Ink Faint
    6. Draw column dividers: 1px Ink Ghost vertical lines at x=220 and x=680 (from y=53 to y=487)

    **resized():**
    Layout the three-column structure:
    ```
    Header: 0-50px (full width)
    Primary: 53-487px (after double-rule gap)
      Left panel: x=0, w=220, padding 16px
      Center panel: x=220, w=460, padding 20px
      Right panel: x=680, w=220, padding 16px
    Footer: 490-530px (full width)
    ```

    **Left panel layout (top to bottom, with 16px padding):**
    - Stage I: Resonance section (y=55): label row (20px), Body knob (56+34=90px), Material dropdown (24px) = ~134px
    - Diamond divider at ~y=200
    - Stage IV: Excitation section: label row (20px), Drive knob (90px) = ~110px
    - Diamond divider at ~y=320
    - Stage V: Room Tone section: label row (20px), Ambience knob (90px), Gate toggle (24px) = ~134px

    **Center panel layout:**
    - Stage II label at top (y=55)
    - Visualization placeholder: 340x300px area (solid Parchment Dark 1px border, "Fig. 1" caption below) -- Phase 7 will fill this
    - Below visualization: Row of room controls
      - Room Size knob (64px), Proximity knob (64px), Width knob (64px) in a row
      - Shape dropdown (full width, below knobs) -- 7 Victorian room names
      - Position: centred horizontally, ~y=410-480

    **Right panel layout (top to bottom, with 16px padding):**
    - Stage III: Air section (y=55): label row (20px), Air knob (90px), Character toggle (24px) = ~134px
    - Diamond divider at ~y=200
    - Stage VI: Diffuse Tail section: label row (20px), Decay knob (90px), Diffusion knob (90px) = ~200px
    - Diamond divider at ~y=410
    - Output section: label row (20px), Mix knob (90px), Level knob (90px) = ~200px
    Note: Tail and Output sections may need compressed layout. Place Decay and Diffusion side-by-side (2 knobs per row at ~100px each = 200px wide fits in 188px with tighter spacing). Same for Mix and Output. Adjust knob width/padding to fit 220px panel.

    Position all knobs centred within their section areas. Single-knob sections: knob centred. Two-knob sections: knobs side by side with ~10px gap. Toggles and dropdowns below their section's knobs.

    Draw diamond dividers between sections in left and right panels using ParchmentElements::drawDiamondDivider in paint().

    **PluginProcessor.cpp** -- Change createEditor():
    Replace `return new juce::GenericAudioProcessorEditor(*this)` with `return new AetherEditor(*this)`. Add `#include "PluginEditor.h"` if not present.

    Add SectionComponent.h/cpp and PresetSelector.h/cpp to CMakeLists.txt target_sources.
  </action>
  <verify>
    Build: `cmake --build build --target Aether_VST3 2>&1 | tail -30` compiles without errors.
    Load plugin in a DAW or standalone. Verify:
    1. Plugin window shows 900x530px with parchment background, paper texture, vignette
    2. Three columns visible with Ink Ghost vertical dividers
    3. Header shows "AETHER" title and "Cosmos Series" mark
    4. All knobs render with parchment gradient body and ink indicator (not default JUCE)
    5. Knob values update when dragged
    6. Bypass buttons toggle sections with visual dimming
    7. All controls respond to mouse interaction
  </verify>
  <done>
    Complete Aether editor with three-column layout, all 21 parameters wired to custom controls (11 knobs, 2 dropdowns, 2 toggles, 6 bypass buttons), header/footer with correct typography, corner brackets, diamond dividers, double-rule borders, paper texture, and vignette. Plugin uses AetherEditor instead of GenericAudioProcessorEditor.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete UI</name>
  <files>Source/PluginEditor.cpp</files>
  <action>
    Human verifies the complete Victorian parchment UI for Aether with all 21 parameters connected, custom knob rendering, toggle switches, bypass buttons, header/footer, ornamental elements, and embedded typography.

    Build and load the plugin in a DAW or run standalone:
    `cmake --build build --target Aether_Standalone`

    Verification checklist:
    1. Layout: 900x530px window with three columns (left 220px, center ~460px, right 220px)
    2. Background: Parchment colour with faint horizontal ruled lines and edge vignette darkening
    3. Header: "AETHER" title with letter spacing, preset dropdown, "COSMOS SERIES" mark
    4. Footer: Version, tagline, brand mark
    5. Knobs: Parchment gradient body with ink indicator line (NOT default JUCE arcs)
    6. Interaction: Drag knobs -- value readout updates in real-time. Try shift-drag for fine control. Double-click to reset. Click on value readout to type a value. Scroll wheel on knobs.
    7. Toggles: Click Air Character options (Warm/Neutral/Cold) -- active option shows ink-inverted
    8. Bypass: Click each section's bypass button. "In"/"Out" text changes. Section dims when bypassed but controls still respond.
    9. Typography: Section labels show Roman numerals (I. through VI.) in correct serif fonts
    10. Ornaments: Corner L-brackets at all four corners. Diamond dividers between side panel sections. Double-rule borders at header/footer.
    11. Value display: Room Size shows "40% (Medium)" format. Decay shows "150 ms" or "1.2s" format. Output Level shows "0.0 dB".
    12. Dropdowns: Material shows 10 options. Shape shows 7 room names.

    Resume signal: Type "approved" if the UI looks correct, or describe specific issues that need fixing.
  </action>
  <verify>Human visually inspects plugin UI in DAW or standalone and confirms all 12 checklist items pass.</verify>
  <done>Human has approved the visual appearance, layout, controls, typography, ornaments, and interaction behavior of the complete Aether UI.</done>
</task>

</tasks>

<verification>
- `cmake --build build --target Aether_VST3` compiles cleanly
- Plugin loads with custom AetherEditor (not GenericAudioProcessorEditor)
- All 21 parameters are wired and functional
- Three-column layout with correct dimensions
- All ornamental elements visible (corners, dividers, double-rules, texture, vignette)
- Typography uses embedded fonts (not system defaults)
- Bypass dimming works without disabling controls
</verification>

<success_criteria>
The plugin displays a complete Victorian parchment UI at 900x530px with three-column layout. All 21 APVTS parameters have connected custom controls. Knobs render with parchment gradient bodies and ink indicators. Toggles show ink-inverted active state. Bypass buttons dim sections while keeping controls interactive. Header/footer, corner brackets, diamond dividers, and double-rule borders are visible. Typography uses Cormorant Garamond, EB Garamond, and Spectral fonts. Human verifies visual correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-interface/06-04-SUMMARY.md`
</output>
