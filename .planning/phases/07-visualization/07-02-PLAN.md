---
phase: 07-visualization
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - Source/PluginEditor.h
  - Source/PluginEditor.cpp
  - CMakeLists.txt
autonomous: false
requirements: [VIZ-01, VIZ-03, VIZ-05]

must_haves:
  truths:
    - "Center column displays the visualization component in the existing placeholder area (~340x260px)"
    - "Visualization reacts in real-time to Room Size, Shape, and Proximity parameter changes"
    - "Wavefront rings animate continuously with visible breathing cycle"
    - "Bypassed stages (Reflections, Air, Excitation, Diffuse Tail) cause their visual elements to fade out smoothly"
    - "Audio-reactive breathing boost is visible when signal passes through the plugin"
    - "Caption reads Fig. 1 -- The Listener below the visualization"
  artifacts:
    - path: "Source/PluginEditor.h"
      provides: "VisualizationComponent member owned by editor"
      contains: "VisualizationComponent"
    - path: "Source/PluginEditor.cpp"
      provides: "Visualization placement, parameter wiring, animation lifecycle"
      contains: "setParameterSources"
    - path: "CMakeLists.txt"
      provides: "VisualizationComponent source files in build"
      contains: "VisualizationComponent"
  key_links:
    - from: "Source/PluginEditor.cpp"
      to: "Source/ui/VisualizationComponent.h"
      via: "addAndMakeVisible + setParameterSources + startAnimation"
      pattern: "vizComponent"
    - from: "Source/PluginEditor.cpp"
      to: "Source/PluginProcessor.h"
      via: "processorRef.visualizationRmsLevel pointer passed to viz"
      pattern: "visualizationRmsLevel"
    - from: "Source/PluginEditor.cpp"
      to: "Source/Parameters.h"
      via: "apvts.getRawParameterValue for viz parameter pointers"
      pattern: "getRawParameterValue"
---

<objective>
Integrate the VisualizationComponent into the editor, wire all parameter sources, manage animation lifecycle, and add build targets.

Purpose: Connects the self-contained visualization to the live plugin state, replacing the placeholder rectangle with the animated Victorian perception diagram. Ensures the visualization starts/stops with the editor lifecycle and responds to all parameter and bypass changes.

Output: Fully functional visualization in the plugin UI, verified by human in standalone build.
</objective>

<execution_context>
@/Users/nathanmcmillan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nathanmcmillan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visualization/07-CONTEXT.md
@.planning/phases/07-visualization/07-01-SUMMARY.md
@Source/PluginEditor.h
@Source/PluginEditor.cpp
@Source/PluginProcessor.h
@Source/ui/VisualizationComponent.h
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate VisualizationComponent into editor and CMake build</name>
  <files>Source/PluginEditor.h, Source/PluginEditor.cpp, CMakeLists.txt</files>
  <action>
**CMakeLists.txt:**
- Add `Source/ui/VisualizationComponent.h` and `Source/ui/VisualizationComponent.cpp` to the JUCE target's source file list (alongside the other ui/ files).

**PluginEditor.h:**
- Add `#include "ui/VisualizationComponent.h"` to includes
- Add private member: `VisualizationComponent vizComponent;` (default constructed)

**PluginEditor.cpp (constructor):**
1. After the existing `addAndMakeVisible (presetSelector)` block (step 8), add:
   ```cpp
   // 8b. Visualization component
   addAndMakeVisible (vizComponent);
   ```

2. After all parameter attachments are set up (after step 9), wire the visualization parameter sources. Get raw atomic pointers from APVTS and the processor's RMS level:
   ```cpp
   // 12. Wire visualization parameter sources
   vizComponent.setParameterSources (
       apvts.getRawParameterValue (ParamIDs::reflSize),
       apvts.getRawParameterValue (ParamIDs::reflShape),
       apvts.getRawParameterValue (ParamIDs::reflProx),
       apvts.getRawParameterValue (ParamIDs::airAmount),
       apvts.getRawParameterValue (ParamIDs::excitDrive),
       apvts.getRawParameterValue (ParamIDs::reflBypass),
       apvts.getRawParameterValue (ParamIDs::airBypass),
       apvts.getRawParameterValue (ParamIDs::excitBypass),
       apvts.getRawParameterValue (ParamIDs::tailBypass),
       &processorRef.visualizationRmsLevel
   );
   vizComponent.startAnimation();
   ```

**PluginEditor.cpp (destructor):**
- Add `vizComponent.stopAnimation();` BEFORE the parameter listener removal (at the very beginning of the destructor). This ensures the timer stops before any other cleanup.

**PluginEditor.cpp (paint):**
- Remove the existing visualization placeholder block (section 9, the `vizBounds` rectangle and "Fig. 1" caption drawing). The VisualizationComponent now handles this as a child component (including the caption).

**PluginEditor.cpp (resized):**
- In the center panel section, add the visualization component bounds. Place it at the existing placeholder position:
  ```cpp
  // Visualization component (replaces placeholder rectangle)
  vizComponent.setBounds (280, 68, 340, 260);
  ```

**Key constraints:**
- Timer MUST stop in destructor before listener removal to prevent dangling pointer access
- Use getRawParameterValue (returns std::atomic<float>*) -- same pattern as processor's cached pointers
- The visualizationRmsLevel is a public member on the processor, accessed via processorRef
- The visualization repaints only itself (340x260px), not the entire 900x530px editor
  </action>
  <verify>
Build the project: `cmake --build build --target Aether 2>&1 | tail -30` -- should compile without errors.

Run standalone: `open build/Aether_artefacts/Standalone/Aether.app` -- visualization should appear in center column with animated breathing.

Verify:
1. Head profile silhouette visible in center column
2. Wavefront rings breathing slowly (4-second pulse)
3. Adjust Room Size knob -- wavefront extent/spacing changes smoothly
4. Switch Shape dropdown -- wave pattern morphs to new fingerprint
5. Adjust Proximity -- source position and opacity balance shifts
6. Toggle Reflections bypass -- wavefronts fade out smoothly, head remains
7. Toggle Air bypass -- particles disappear
8. Toggle Excitation bypass -- stars disappear
9. Toggle Diffuse Tail bypass -- scatter disappears
10. Caption "Fig. 1 -- The Listener" visible below visualization
  </verify>
  <done>
VisualizationComponent is a visible child of the editor, positioned at (280, 68, 340, 260). All 10 parameter sources wired. Animation starts on editor creation, stops on destruction. Placeholder rectangle and caption removed from editor paint(). Build compiles and runs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify visualization rendering, animation, and parameter reactivity</name>
  <action>
Human verifies the complete acoustic visualization in the center column of the Aether plugin. The visualization shows a Victorian head profile silhouette with concentric wavefront rings that breathe slowly, react to Room Size/Shape/Proximity parameters, and display per-stage visual elements (wavefronts, air particles, excitation stars, diffuse scatter) that fade out when their stage is bypassed.

Steps:
1. Build and run standalone: `cmake --build build --target Aether && open build/Aether_artefacts/Standalone/Aether.app`
2. Visual presence: center column shows head profile, wavefront rings, "Fig. 1 -- The Listener" caption, all in monochrome ink
3. Breathing animation: wavefront rings pulse subtly on ~4-second cycle
4. Room Size: small = tight/close wavefronts, large = wide/spacious wavefronts
5. Shape: each of 7 shapes has distinct wave pattern, smooth morph transitions
6. Proximity: near = strong direct waves, far = flatter/scattered patterns
7. Bypass: each stage toggle fades its visual layer, head remains as anchor
8. Performance: no frame drops or stutter
  </action>
  <verify>Human confirms visual correctness by typing "approved" or describing issues.</verify>
  <done>Human has approved the visualization rendering, animation, and parameter reactivity.</done>
</task>

</tasks>

<verification>
1. CMakeLists.txt includes VisualizationComponent.h and .cpp
2. PluginEditor owns a VisualizationComponent member
3. Visualization placed at (280, 68, 340, 260) in resized()
4. setParameterSources called with all 10 atomic pointers
5. startAnimation() called in constructor, stopAnimation() in destructor
6. Placeholder rectangle and "Fig. 1" paint code removed from editor
7. Project builds without errors
8. Human verifies visual rendering, animation, and parameter reactivity
</verification>

<success_criteria>
- Plugin builds and loads with the visualization visible in the center column
- Head profile, wavefronts, and caption render correctly in monochrome ink
- Breathing animation runs continuously at ~4-second cycle
- Room Size, Shape, and Proximity cause smooth visual changes
- Bypass toggles cause corresponding visual elements to fade out
- Audio input causes visible breathing boost
- Human approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/07-visualization/07-02-SUMMARY.md`
</output>
