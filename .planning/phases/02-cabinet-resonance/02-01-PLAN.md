---
phase: 02-cabinet-resonance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/Parameters.h
  - Source/dsp/ResonanceSection.h
  - Source/dsp/ResonanceSection.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - CAB-02
  - CAB-03

must_haves:
  truths:
    - "Parameter IDs use res_weight, res_material, res_bypass naming"
    - "Material selector has 10 material choices (4 woods, 3 metals, 3 stones)"
    - "Display names use 'I Resonance' prefix"
    - "ParameterID version is 2 for all Stage I parameters"
    - "CabinetSection renamed to ResonanceSection in all files"
    - "Plugin builds and loads with updated parameters"
  artifacts:
    - path: "Source/Parameters.h"
      provides: "Updated param IDs, 10-material choice list, version 2"
      contains: "res_weight"
    - path: "Source/dsp/ResonanceSection.h"
      provides: "Renamed stage class with FDN member declarations"
      contains: "class ResonanceSection"
    - path: "Source/dsp/ResonanceSection.cpp"
      provides: "Renamed implementation file"
      contains: "ResonanceSection::"
    - path: "Source/PluginProcessor.h"
      provides: "Updated includes, member names, param pointers"
      contains: "ResonanceSection"
    - path: "Source/PluginProcessor.cpp"
      provides: "Updated param caching and updateStageParams"
      contains: "resWeightParam"
    - path: "CMakeLists.txt"
      provides: "Updated source file references"
      contains: "ResonanceSection"
  key_links:
    - from: "Source/PluginProcessor.cpp"
      to: "Source/Parameters.h"
      via: "ParamIDs::resWeight cached to atomic pointer"
      pattern: "resWeightParam.*getRawParameterValue.*resWeight"
    - from: "Source/PluginProcessor.cpp"
      to: "Source/dsp/ResonanceSection.h"
      via: "resonanceSection member and process call"
      pattern: "resonanceSection\\.process"
---

<objective>
Refactor Stage I parameters from "Cabinet" to "Resonance" naming and rename CabinetSection to ResonanceSection. Expand the material selector from 3 choices to 10 materials across wood, metal, and stone families.

Purpose: Establishes the correct parameter infrastructure and naming that the FDN DSP implementation (Plan 02) builds on. The ID rename, version bump, and choice expansion must happen atomically before DSP work begins.
Output: Renamed ResonanceSection class, updated Parameters.h with 10 materials, updated PluginProcessor references, building plugin with new parameter names visible in DAW.
</objective>

<execution_context>
@/Users/nathanmcmillan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nathanmcmillan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-audio-pipeline/01-01-SUMMARY.md
@.planning/phases/01-project-scaffold-audio-pipeline/01-02-SUMMARY.md
@.planning/phases/02-cabinet-resonance/02-CONTEXT.md
@.planning/phases/02-cabinet-resonance/02-RESEARCH.md
@Source/Parameters.h
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/dsp/CabinetSection.h
@Source/dsp/CabinetSection.cpp
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CabinetSection to ResonanceSection and update all references</name>
  <files>
    Source/dsp/ResonanceSection.h
    Source/dsp/ResonanceSection.cpp
    Source/PluginProcessor.h
    Source/PluginProcessor.cpp
    CMakeLists.txt
  </files>
  <action>
    1. Create Source/dsp/ResonanceSection.h by copying CabinetSection.h content:
       - Rename class from `CabinetSection` to `ResonanceSection`
       - Update header comment to: "Stage I: Resonance -- Short FDN for material body resonance with resonant bandpass filtering. 10 material types across wood, metal, and stone families."
       - Update include guard to `#pragma once` (already there)
       - Keep the same public interface: prepare/process/reset/setBypass
       - Add forward declarations for FDN members (these will be filled in Plan 02):
         - `float weight = 0.5f;` (current weight value)
         - `int materialIndex = 3;` (default: Mahogany, index 3)

    2. Create Source/dsp/ResonanceSection.cpp by copying CabinetSection.cpp content:
       - Update all `CabinetSection::` to `ResonanceSection::`
       - Update include to `#include "ResonanceSection.h"`
       - Keep placeholder implementations (Plan 02 fills in real DSP)
       - Update TODO comments to reference "Resonance FDN" instead of "cabinet"

    3. Delete Source/dsp/CabinetSection.h and Source/dsp/CabinetSection.cpp (use git rm)

    4. Update CMakeLists.txt:
       - Replace `Source/dsp/CabinetSection.cpp` with `Source/dsp/ResonanceSection.cpp`
       - Replace `Source/dsp/CabinetSection.h` with `Source/dsp/ResonanceSection.h`

    5. Update Source/PluginProcessor.h:
       - Change `#include "dsp/CabinetSection.h"` to `#include "dsp/ResonanceSection.h"`
       - Rename member `CabinetSection cabinetSection;` to `ResonanceSection resonanceSection;`
       - Rename cached param pointers:
         - `cabBodyParam` to `resWeightParam`
         - `cabTypeParam` to `resMaterialParam`
         - `cabBypassParam` to `resBypassParam`
       - Update comments from "Cabinet" to "Resonance"

    6. Update Source/PluginProcessor.cpp:
       - Update all references to `cabinetSection` to `resonanceSection`
       - Update param caching in constructor:
         - `resWeightParam = apvts.getRawParameterValue(ParamIDs::resWeight);`
         - `resMaterialParam = apvts.getRawParameterValue(ParamIDs::resMaterial);`
         - `resBypassParam = apvts.getRawParameterValue(ParamIDs::resBypass);`
       - Update updateStageParams():
         - `resonanceSection.setBypass(resBypassParam->load() >= 0.5f);`
       - Update prepareToPlay: `resonanceSection.prepare(...)`
       - Update releaseResources: `resonanceSection.reset()`
       - Update processBlock: `resonanceSection.process(buffer)`
  </action>
  <verify>
    Run `cmake --build build --config Debug 2>&1 | tail -20` -- project must compile with zero errors. Verify no references to CabinetSection remain: `grep -r "CabinetSection" Source/` should return nothing. Verify no references to old param names remain: `grep -r "cabBody\|cabType\|cabBypass" Source/` should return nothing (except possibly comments referencing the rename).
  </verify>
  <done>CabinetSection fully renamed to ResonanceSection across all source files. CMakeLists.txt updated. PluginProcessor uses new member names and param pointer names. Project compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update Parameters.h with new IDs, display names, 10-material choice list, and version 2</name>
  <files>Source/Parameters.h</files>
  <action>
    Update Source/Parameters.h with the following changes:

    1. Rename ParamIDs namespace entries for Stage I:
       - `cabBody` to `resWeight` with value `"res_weight"`
       - `cabType` to `resMaterial` with value `"res_material"`
       - `cabBypass` to `resBypass` with value `"res_bypass"`
       - Update section comment to "Stage I: Resonance"

    2. Update the Stage I parameter definitions in createParameterLayout():

       a. Weight parameter (was Body):
          - ParameterID: `{ ParamIDs::resWeight, 2 }` (version 2 -- breaking change from cab_body)
          - Display name: `"I Resonance - Weight"`
          - Range: NormalisableRange<float>(0.0f, 1.0f, 0.01f)
          - Default: 0.5f
          - String format: same percentage display (value * 100 + "%")

       b. Material parameter (was Type):
          - ParameterID: `{ ParamIDs::resMaterial, 2 }` (version 2)
          - Display name: `"I Resonance - Material"`
          - Choice list (10 items): `"Pine", "Oak", "Walnut", "Mahogany", "Iron", "Steel", "Copper", "Limestone", "Marble", "Granite"`
          - Default index: 3 (Mahogany -- a warm, musical default)

       c. Bypass parameter:
          - ParameterID: `{ ParamIDs::resBypass, 2 }` (version 2)
          - Display name: `"I Resonance - Bypass"`
          - Default: false (same as before)

    3. Leave ALL other stage parameters untouched (Stages II-VI and Output remain at version 1 with original IDs).

    IMPORTANT: The version bump from 1 to 2 on Stage I parameters is intentional. This is a semantic change (ID rename + choice expansion). Aether is pre-release so no backward compatibility concern exists.
  </action>
  <verify>
    Run `cmake --build build --config Debug 2>&1 | tail -20` -- must compile cleanly. Verify the parameter IDs: `grep -n "res_weight\|res_material\|res_bypass" Source/Parameters.h` should show 3 matches in ParamIDs namespace. Verify material count: `grep -c "Pine\|Oak\|Walnut\|Mahogany\|Iron\|Steel\|Copper\|Limestone\|Marble\|Granite" Source/Parameters.h` should return 10 (one per material in the StringArray).
  </verify>
  <done>Parameters.h has res_weight/res_material/res_bypass IDs at version 2. Material choice has 10 items across 3 families. Display names use "I Resonance" prefix. Plugin compiles and all 19 parameters load correctly.</done>
</task>

</tasks>

<verification>
- `cmake --build build --config Debug` completes with zero errors
- `grep -r "CabinetSection" Source/` returns no matches
- `grep -r "cab_body\|cab_type\|cab_bypass" Source/` returns no matches in active code
- Source/dsp/CabinetSection.h and .cpp no longer exist
- Source/dsp/ResonanceSection.h and .cpp exist with correct class name
- Parameters.h contains 10 material names in the choice list
- All Stage I ParameterIDs use version 2
</verification>

<success_criteria>
- ResonanceSection class exists and is used throughout the codebase
- Parameter IDs are res_weight, res_material, res_bypass
- Material selector offers 10 choices
- ParameterID version is 2 for all three Stage I parameters
- Plugin builds and loads in DAW with updated parameter names
</success_criteria>

<output>
After completion, create `.planning/phases/02-cabinet-resonance/02-01-SUMMARY.md`
</output>
